# copie para .env e ajuste
DATABASE_URL=postgres://modelagens:modelagens@localhost:5432/modelagens
# banco que a suíte de testes APAGA e recria a cada rodada
DATABASE_URL_TESTE=postgres://modelagens:modelagens@localhost:5432/modelagens_teste
PLATAFORMA_NOME=Modelagens
URL_PUBLICA=http://localhost:3000
PORTA=3000
# sem SMTP_URL, os e-mails (recuperação de senha) saem no log do servidor
# SMTP_URL=smtps://usuario:senha@smtp.exemplo.com:465
EMAIL_REMETENTE=Modelagens <nao-responda@exemplo.com>
